<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600" style="background:#f8fafc">
  <!-- Desk -->
  <rect x="100" y="400" width="600" height="20" fill="#334155" rx="2"/>
  <rect x="120" y="380" width="560" height="20" fill="#475569" rx="2"/>
  
  <!-- Laptop Base -->
  <rect x="250" y="320" width="300" height="20" fill="#1e293b" rx="2"/>
  <rect x="260" y="340" width="280" height="10" fill="#0f172a" rx="1"/>
  
  <!-- Laptop Screen -->
  <rect x="200" y="140" width="400" height="240" fill="#1e293b" rx="8"/>
  <rect x="220" y="160" width="360" height="200" fill="#0f172a" rx="4"/>
  
  <!-- Screen Content -->
  <rect x="240" y="180" width="320" height="160" fill="#1e40af" opacity="0.1"/>
  
  <!-- Code Editor UI -->
  <rect x="240" y="190" width="320" height="30" fill="#334155" rx="2"/>
  <circle cx="260" cy="205" r="5" fill="#ef4444"/>
  <circle cx="280" cy="205" r="5" fill="#fbbf24"/>
  <circle cx="300" cy="205" r="5" fill="#22c55e"/>
  
  <!-- Code Text -->
  <text x="260" y="230" font-family="monospace" font-size="14" fill="#e2e8f0">const api = new BansanAPI();</text>
  <text x="260" y="250" font-family="monospace" font-size="14" fill="#e2e8f0">async function loadData() {</text>
  <text x="280" y="270" font-family="monospace" font-size="14" fill="#e2e8f0">try {</text>
  <text x="300" y="290" font-family="monospace" font-size="14" fill="#e2e8f0">const response = await api.get();</text>
  <text x="280" y="310" font-family="monospace" font-size="14" fill="#e2e8f0">} catch (error) {</text>
  <text x="300" y="330" font-family="monospace" font-size="14" fill="#e2e8f0">console.error(error);</text>
  
    
   <!-- Subtle Shadow -->
  <ellipse cx="400" cy="420" rx="300" ry="20" fill="#000" opacity="0.05"/>
</svg>